<Window x:Class="BaseLib.Wpf.FilterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="FilterWindow" Height="500" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <ToolBarTray>
            <ToolBar>
                <Button Width="20" Height="20" x:Name="AddButton" ToolTip="Add" Click="AddButton_OnClick">
                    <Image x:Name="I1" />
                </Button>
                <Button Width="20" Height="20" x:Name="RemoveButton" ToolTip="Remove" Click="RemoveButton_OnClick">
                    <Image x:Name="I2" />
                </Button>
            </ToolBar>
        </ToolBarTray>
        <Grid Grid.Row="1" Background="#FFFFFF">
            <ScrollViewer  x:Name="ScrollPanel1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
